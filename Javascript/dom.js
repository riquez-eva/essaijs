//Exercice DOM

/*let x = window.parseInt(prompt("entrez le nombre de valeurs pour votre tableau"));
let tab =[];

for (i = 0; i < x; i++) {
    let valeur = parseInt(prompt("entrez le nombre:" + (i + 1)));
    tab.push(valeur);
    console.log("voici les valeurs rentrées :", valeur);

    if (valeur == isNaN){
        alert("rentrez unniquement des nombres");
    }
}
console.log(tab);

function moyenne(tab){
    let somme = 0;

        for (let i = 0; i< tab.length; i++){
        somme += tab[i];   
        console.log("voici la somme", somme);
    }
    return somme / tab.length;
    
}
let moy = moyenne(tab);



console.log("voici la moyenne", moyenne(tab));*/